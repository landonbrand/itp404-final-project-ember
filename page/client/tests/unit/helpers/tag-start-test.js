
import { tagStart } from 'page/helpers/tag-start';
import { module, test } from 'qunit';

module('Unit | Helper | tag start');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagStart([42]);
  assert.ok(result);
});

