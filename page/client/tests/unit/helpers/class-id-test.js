
import { classId } from 'page/helpers/class-id';
import { module, test } from 'qunit';

module('Unit | Helper | class id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classId([42]);
  assert.ok(result);
});

